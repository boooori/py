#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(){
	/*
	srand(time(NULL));
	int random = 0;
	int num[0];
	for(int i = 0;i<10;i++)
		random = rand() % 9;
		num[i] = {random};
		printf("%d",num);
		*/
	int num1,num2 = 0;
	scanf("%d,%d",&num1,&num2);
	return 0;
	
}
